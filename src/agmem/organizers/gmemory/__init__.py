"""G-Memory: MAS trajectory sparsification into insights, reward-based pruning.

``GMemoryOrganizer`` is re-exported here, so ``from agmem.organizers.gmemory import GMemoryOrganizer``
resolves exactly as it did when this was a single module."""

from agmem.organizers.gmemory.organizer import GMemoryOrganizer

__all__ = ["GMemoryOrganizer"]
