# The defect ledger — what the source papers' own code does

This repository reimplements ten upstream artifacts: the nine memory methodologies listed in the README plus A-MAC's admission policy. Doing that faithfully required reading each paper against its official repository line by line, and the recurring outcome was not a transcription difficulty but a factual one — **the paper and the code frequently describe different systems, and in several cases the published number is an artifact of a defect rather than of the mechanism the paper argues for.** This document is the ledger of those findings. It is not a survey of the literature and it is not a criticism of the projects involved; every one of them is serious work, and research code that drifts from its paper is the ordinary condition of the field. It is, however, the reason "be faithful to the paper" is not a well-defined target, and the reason this repo pins lineages instead.

The evidence standard is fixed and applies to every entry below. Each claim cites the upstream `file:line` at a **pinned SHA** — the snapshot table gives the full hashes, and a proof against any other SHA proves something else. Each entry then carries a proof cell naming one of three things: a Tier-0 reproduction script under `scripts/repro/defects/`, which CI runs against the pinned snapshots on pushes to `main` and on every pull request, and which spends $0 and makes no model call; a pinning test in `tests/`; or the research document that established the finding, in which case the finding also passed the adversarial verification round described below. Findings originated in twelve audit rounds recorded under `docs/research/`; the final round ran eight parallel fresh-eyes audits that read the upstream clones directly and treated this repo's own documentation as claims to be broken rather than as evidence. Those findings then went through a verification pass whose explicit mandate was to refute them, using code-line citation and deterministic reproduction only: **96 verdicts, 94 confirmed and 2 sub-claims refuted.** The refutations were honored: both overturned sub-claims were withdrawn rather than softened, and neither survives into this ledger. Ten entries below additionally record a correction to *our own* earlier claim — A-3, B-3, B-5, B-6, B-8, C-2, C-3, C-4, C-7 and C-8 — because a ledger that only catalogues other people's errors is not an audit. B-8 is the sharpest of them, and the most recent: it was published as a Tier C entry blaming *our* harness, and withdrawn the same day once the pinned clone showed the step belonged to upstream. What made that possible was reading the paper's description of a read path and reasoning about it as though it were the code's.

The full per-entry evidence base — **131 numbered findings across twelve sections, 83 of them carrying an impact tag** — lives in [`docs/research/upstream-defect-catalog.md`](research/upstream-defect-catalog.md). This ledger reorganizes the load-bearing subset into three tiers.

---

## How to read an entry

Every entry except C-5 — which collects short citation defects that have no single code site — uses the same five-field schema:

| Field | What it holds |
|---|---|
| **Paper claim** | what the paper (or the release's own documentation) says the mechanism is |
| **Official code** | what the released code actually does, with `file:line` at the pinned SHA |
| **Our handling** | the fix, or the deliberate deviation — disclosed at the code site, not only here |
| **Proof** | a `scripts/repro/defects/*.py` script run by CI, a pinning test, or the research doc plus its verification verdict |
| **Impact** | what this does to the published numbers, stated no more strongly than the evidence supports |

The tiers order entries by how much they cost a reader who wants to cite these systems:

- **Tier A — the published number cannot be taken at face value from the released code.** This comes in three shapes: the reported result is produced by the defect rather than by the mechanism the paper credits (A-1); the artifact that would reproduce it does not run, or is not the path the repository ships by default (A-2, A-3); or the code that produced it was never released at all, because the write path runs inside a hosted service (A-4).
- **Tier B — paper ≠ code.** The mechanism the paper describes is absent, inert, or numerically different in the code, but the effect on any specific published number is not established.
- **Tier C — the evaluation harness itself.** Defects in the scoring and benchmark code, which move numbers for everyone who uses them, including us.

A note on what this ledger does *not* do. Entries tagged in the catalog as latent — dead code with no path to a published number — stay latent here. Where our own earlier claim was stronger than the evidence, the walked-back version is what appears below.

---

## Pinned upstream snapshots

Every citation to a *methodology's* code in this document resolves against these commits. CI fetches five of them at exactly these SHAs — `AgenticMemory`, `GMemory`, `MemMachine`, `nemori` and `reasoning-bank`, the five the Tier-0 scripts actually read (`.github/workflows/ci.yml`). All nine are kept locally under `$AGMEM_UPSTREAM` (default `~/.agmem/upstream`), and those same five pins are duplicated in `scripts/repro/defects/_common.py` as `PINS`, which warns loudly if a local clone has moved. Licenses were read from the LICENSE file present in each snapshot.

| Local name | Repository | Pinned SHA | License at pin |
|---|---|---|---|
| `AgenticMemory` | [WujiangXu/AgenticMemory](https://github.com/WujiangXu/AgenticMemory) — A-Mem, paper-repro edition | `0c8039f28fdcc08189a23c07a3437d9d2482f9c2` | MIT |
| `nemori` | [nemori-ai/nemori](https://github.com/nemori-ai/nemori) | `d2a6dff6e5481214a0be6a2d10147feccfc16244` | MIT |
| `MemoryOS` | [BAI-LAB/MemoryOS](https://github.com/BAI-LAB/MemoryOS) | `587ed7755c7aed179965792830ff1b5ad9a6fa92` | Apache-2.0 |
| `graphiti` | [getzep/graphiti](https://github.com/getzep/graphiti) — Zep | `9140123a7282d44efc077a0af09179919f3defdf` | Apache-2.0 |
| `GMemory` | [bingreeky/GMemory](https://github.com/bingreeky/GMemory) | `7b581c51d993bd600df14691d101d7e601040cc6` | **none** — no LICENSE file at this SHA |
| `ace` | [ace-agent/ace](https://github.com/ace-agent/ace) | `bcb7cea0504afad6f55fec4845dd4864c9f9eee7` | Apache-2.0 |
| `reasoning-bank` | [google-research/reasoning-bank](https://github.com/google-research/reasoning-bank) | `ed80611788292ea739f1effd31f16c53823b8a0d` | Apache-2.0 |
| `MemMachine` | [MemMachine/MemMachine](https://github.com/MemMachine/MemMachine) | `18f1211290c50ae30e9960b90bbe57d89bf68600` | Apache-2.0 |
| `amac` | [GuilinDev/Adaptive_Memory_Admission_Control_LLM_Agents](https://github.com/GuilinDev/Adaptive_Memory_Admission_Control_LLM_Agents) | `40407aec883bf289664653406ac15a4cac1f6158` | MIT |
| `mem0` @ tag `v0.1.94` | [mem0ai/mem0](https://github.com/mem0ai/mem0) — the paper-era OSS library | `07ddd7cb4bd67962cf9a988d7b5c3f3920fad2d4` | Apache-2.0 |
| `mem0` @ tag `evaluation-archive` | same repository — the preserved paper harness | `931d579ba56cf6ef0c50cdb0495ce79a41d15def` | Apache-2.0 |

Two traps live in that pair of Mem0 rows, and both are the kind this table exists to prevent.
First, `evaluation-archive` is an **annotated** tag: `git rev-parse evaluation-archive` returns
`f65142f6…`, the tag *object*, and only `git rev-parse evaluation-archive^{commit}` returns the
`931d579b…` above. A check written without the peel compares a commit against a tag object and
reports a mismatch on a clone that is in fact correct. Second, the repository carries two tag
series distinguished only by a `v` prefix, and they are not the same lineage — the unprefixed
`0.1.94` is a commit from **2024-03-05**, sixteen months before the tag we pin. Any citation of
"Mem0 at 0.1.94" that does not show the `v` is ambiguous.

The G-Memory row is not an oversight on our part: the repository carries no license of any kind, re-confirmed via the GitHub API on 2026-07-27 (`license: null`). We read it, cite it, and reimplement from it; we vendor nothing.

Two repositories cited in Tier C are **not** in this table and are not pinned locally: `snap-research/locomo`, for which the catalog records no license, and `xiaowu0162/LongMemEval`, which it records as MIT. Their citations come from the research docs that transcribed the relevant functions with `file:line`, and — where a Tier-0 proof exists for that area — it runs against *our own committed result artifacts* rather than against an unpinned clone. The distinction is stated rather than smoothed over, because it is exactly the kind of gap this ledger exists to name.

Three of these projects ship more than one copy of their own core, and the copies disagree on constants *and* formulas — MemoryOS has three (`memoryos-pypi/`, `memoryos-chromadb/`, `eval/`), A-Mem has three repositories plus a plain-vs-robust split inside one of them, MemMachine has two backends across two harnesses. Wherever that matters below, the entry names the copy.

---

## Tier A — the published number is an artifact

### A-1 · A-MAC: recall 0.972 is a product of a substring bug

| | |
|---|---|
| **Paper claim** | An adaptive admission gate scores each candidate on five weighted features — novelty, recency, type prior, coherence, utility — and admits above θ\* = 0.55, reporting recall 0.972 / precision 0.417 / F1 0.583. |
| **Official code** | Three of the five features do not behave as described. The type prior matches its keyword sets as **bare substrings** (`features/type_prior.py:129`, `if keyword in content`), and the `fact` set contains `{'is','are','was','were',…}` (`:69-72`, prior 0.7 at `:38`) — so `'is'` matches *sister*, *this*, *island*, and nearly every substantive turn classifies as `fact`. Novelty is pinned at 1.0 on both executable paths, because `extract_features_for_candidates` passes `existing_memories=[]` (`optimize_weights_cv.py:78`, `run_all_baselines.py:98`) and `features/novelty.py:47-49` returns 1.0 on an empty list — the README-advertised `all-MiniLM-L6-v2` is never called. Recency is ≈ 0 for every candidate: `current_time=time.time()` against LoCoMo timestamps seeded at 2023-05-01 (`data_loader.py:110-119`) gives age 28,378 h and `exp(−0.01·28378) = 5.7e-124`. With the release weights `[0.1,0.1,0.1,0.1,0.6]`, θ = 0.55, N = 1, R = 0 and the utility fallback 0.5, the score reduces to `S = 0.57 + 0.1·C ≥ θ` **always** for `fact`/`preference`/`identity`; only `unknown` (T = 0.5) would need C ≥ 1.0, which is unreachable. The published gate is therefore *"admit unless the turn is a bare interjection."* |
| **Our handling** | Not fixed — this *is* the artifact, so it is reproduced and isolated instead. Two configs bracket it: `amem_amac` (`matching="word"`, whole token or phrase) is our default, `amem_amac_upstream` (`matching="substring"`) is byte-exact release reproduction. Novelty is scored against the run's real vector index (ANN top-1 per type, which is the formula's `max`); recency is reproduced faithfully with `decay_rate` exposed and `AdmissionStats` recording feature means, so the deadness is **observed in our runs, not assumed**. Each deviation is declared in `policies/admission.py`. |
| **Proof** | `tests/test_admission_gate.py:499` (`test_substring_matching_admits_more_which_is_defect_2_in_our_own_pipeline`) pins the behavioral difference end to end: on a 5-turn facade run, ungated ingest produces 5 notes / 9 organizer LLM calls / admit rate 1.00; word matching gives 2 / 3 / 0.40; substring matching — the release — gives 3 / 5 / 0.60, and the single extra admitted turn is exactly the *"…with my sister…"* turn, admitted via `s-**is**-ter`. Substring mode was separately verified against the upstream classifier imported standalone: **5015/5015 classify-and-score matches**, equal priors, equal keyword sets element for element, identical iteration order and tie-breaks (catalog §1, §11). |
| **Impact** | **The strongest single claim in this ledger.** Recall 0.972 follows from admitting nearly everything, and precision 0.417 barely exceeds the base rate of the label itself (A-Mem's 0.371 on the same table). The paper's "type prior dominates" ablation was additionally run with two of the five features constant. |

One caveat travels with any citation of A-MAC's numbers, and it is not a defect so much as a definition: the reported F1 is an **admission-decision** F1 over oracle labels (N = 225) back-derived from future QA — `data_loader.get_memory_candidates` sets `is_referenced` from the union of all QA evidence — not an answer F1. A-Mem's recall of 1.000 in that table is 1 by definition, since it stores everything, and the label cannot exist at deployment time. A-MAC's 0.583 must therefore never be tabulated beside A-Mem Table 1's answer F1. Our own success criterion for this gate is stated differently: answer quality held while note count and write tokens fall, with `admission=None` as the control.

### A-2 · A-Mem: the published-numbers repository's default path stores empty metadata, after paying for it

| | |
|---|---|
| **Paper claim** | Eq. (1)–(2): every note carries LLM-extracted keywords, context and tags, and those fields participate in the note's embedding and in link formation. |
| **Official code** | In the paper-repro repository's plain `memory_layer.py`, `re` is never imported (import block at `:1-19`), but `analyze_content` calls `re.sub` at `:380` after the LLM has responded. The resulting `NameError` lands in a bare `except:` at `:382`, whose handler immediately references an undefined `e` at `:383` and raises a *second* `NameError` — before its own fallback dict at `:385-389` is ever assigned. Control therefore reaches the outer handler at `:393`, which returns the four-key dict at `:396-401`: `keywords: []`, `context: "General"`, `category: "Uncategorized"`, `tags: []`. **Every note therefore ends up with empty metadata, after the extraction call has already been spent.** Only `memory_layer_robust.py` parses correctly (`:317-345`). |
| **Our handling** | Not applicable to our port — we always run the extraction step and parse its output. The finding was added to the reproduction checklist as a precondition: *confirm which path you are on before comparing to a published number.* |
| **Proof** | `scripts/repro/defects/repro_amem_nameerror.py`, run by CI against the pinned snapshot. Its static half is an AST proof that `re.*` is used and `re` is never imported, so the `NameError` is unavoidable. Its dynamic half imports the clone's own module, calls the real `analyze_content` with a stub LLM returning *perfectly valid* JSON, and asserts the return value is the empty-metadata fallback after **exactly one** LLM call. |
| **Impact** | The shipped default path of the repository that produced the paper's numbers yields notes with no metadata at all. Corroborated externally by upstream issues #24 ("all metrics zero") and #5. |

This one is inseparable from the repo-multiplicity problem. Three official A-Mem repositories exist: the paper-repro repository (whose numbers come from the *robust* path), the system edition, and the library edition that the arXiv page links to. The library edition never calls the extractor at all — `add_note` does not invoke `analyze_content`, so its step-1 extraction is dead code (issue #10, partially patched by #13, still open). Any A-Mem reproduction must state which of the three it ran and, inside the first, which of the two parsers.

### A-3 · MemMachine: no evaluation entry point executes at the audited SHA

| | |
|---|---|
| **Paper claim** | LoCoMo and LongMemEval results produced by the evaluation harness shipped in the repository. |
| **Official code** | `evaluation/utils/agent_utils.py:452-459` *calls* `LongTermMemoryParams(...)`, but at this SHA that name is a type expression, not a class: `Annotated[DeclarativeBackendParams \| EventBackendParams, Field(discriminator="backend")]` (`long_term_memory.py:162-165`). Python raises `TypeError: 'types.UnionType' object is not callable` at the typing layer, before pydantic ever sees the keyword arguments — so the failure is pydantic-version-independent. Every eval entry point routed through `init_memmachine_params` dies at construction, and the legacy scripts' separate tuple-unpack `ValueError` is never even reached. |
| **Our handling** | Documented, with both shipped operating points registered on our side as explicit configs (`memmachine` = the legacy `limit=30, expand_context=3`; `memmachine_library` = the library defaults `20 / 0`), since they are different systems and neither is "the" MemMachine. Our own earlier note claiming the retrieval-agent path was the recommended and therefore working entry point was wrong and was corrected (commit `1f4df42`). |
| **Proof** | `scripts/repro/defects/repro_memmachine_typeerror.py`. The static half asserts both source facts in the pinned clone; the dynamic half rebuilds the exact construct with pydantic and catches the `TypeError`. |
| **Impact** | The published numbers necessarily predate the union refactor, and the harness that would reproduce them does not run at HEAD of a 3.3k-star repository. |

### A-4 · Mem0: the LoCoMo numbers were produced by a closed hosted service, not by the released library

*(The Mem0 study files this as **M0-C1**; it is A-4 here because this ledger numbers by tier.)*

| | |
|---|---|
| **Paper claim** | arXiv:2504.19413 reports Mem0's LoCoMo results as the output of the evaluation harness shipped with the paper. |
| **Official code** | That harness's ingest entry point constructs `MemoryClient(api_key=…, org_id=…, project_id=…)` (`evaluation/src/memzero/add.py:47-51` @ `evaluation-archive`) and calls `self.mem0_client.add(message, user_id=…, version="v2", …)` (`:69-71`); the search side is the same client (`evaluation/src/memzero/search.py:20-24`). `MemoryClient` is a pure HTTP client — `self.host = host or "https://api.mem0.ai"` (`mem0/client/main.py:117`), `self.client.post("/v3/memories/add/", json=payload)` (`:217`) — with no extraction or update logic on the client side at all. **The write path that produced the published table is server-side and not released.** The repository's own `evaluation/README.md:3,6` identifies this harness as the paper's code and carries its BibTeX, so the identification is upstream's, not ours. The current benchmarks repository does not close the gap: `results/` holds LoCoMo results only under `results/platform/`, and `results/oss/` contains LongMemEval files and **no LoCoMo at all**. |
| **Our handling** | We port the paper-era OSS two-phase write (`v0.1.94`) and compare **structure**, never claiming to reproduce the published value. The port's own deviations are declared at the code site (`src/agmem/organizers/mem0/organizer.py`): one store per conversation rather than upstream's two per speaker, and `k = 30` at read time. The comparison table carries the closed-write-path footnote as a hard requirement. |
| **Proof** | `docs/research/mem0.md` §① (static read of the pinned clone, every claim `file:line`), plus this track's own run artifacts under `results/repro/` — see [`18-locomo-4way.md`](18-locomo-4way.md). No Tier-0 script: the finding is a lineage argument across two files, which the ledger's evidence standard admits as "the research document plus its verification verdict" rather than as a runnable assertion about a value. |
| **Measured** | Our port of the OSS structure scores **J 31.82 / F1 24.71** on LoCoMo 10conv under this repo's harness at `text-embedding-3-small`, against A-Mem 59.87 and Nemori 65.78-67.60 measured identically. The deficit decomposes: Mem0 abstained on **55.6%** of judged questions versus 19.5-26.9% for the other arms, and was correct on 71.7% of the ones it did answer. Retrieval returned a full k=30 on all 1,986 questions and was never empty, so this is a property of the write path's output granularity — an atomic fact averaging 46.0 characters across all 5,427 of them — not a retrieval failure. |
| **Impact** | The most-cited number for the most-starred system in this comparison cannot be checked against any released code. This is not a claim that the number is wrong; it is a claim that **no one outside the vendor can verify it**, which is a different and more durable problem. Our row is not evidence against the published value either — it measures a different write path, and says so. |

Two further hazards travel with this entry. The first is a false-corroboration trap: an independent
re-measurement reporting **32.4%** against a Mem0 vendor claim of 93.4% circulates in this area, and
our 31.82 is numerically adjacent to it — but that re-measurement is **LongMemEval** and ours is a
LoCoMo judge accuracy, so pairing them would manufacture agreement out of unrelated experiments.
The second is structural: the main repository's `evaluation/` is an **uninitialized submodule**
pointing at `mem0ai/memory-benchmarks`, so a reader who clones `mem0` and opens `evaluation/` sees
an empty directory. The lineage question this entry answers is invisible by default.

### A-5 · ReasoningBank: neither half of MaTTS is runnable from the released code

| | |
|---|---|
| **Paper claim** | MaTTS (§3.3) is the paper's second contribution and its scaling axis: **parallel** scaling generates *k* trajectories per query and distils memory from their self-contrast; **sequential** scaling refines one trajectory in place. Figure 4 reports both on WebArena-Shopping at k=5 — parallel 55.1, sequential 54.5, against 49.7 at k=1 — and §4.4's Pass@k, Best-of-N and vanilla-TTS comparisons all rest on them. |
| **Official code** | **Sequential has no implementation.** `SEQUENTIAL_PROMPT` and `SEQUENTIAL_FOLLOWING_PROMPT` are defined in `WebArena/prompts/memory_instruction.py:132,139` and referenced **nowhere else in the snapshot** — no runner, no pipeline, no agent. `pipeline_scaling.py` is parallel-only. **Parallel is implemented and crashes.** `induce_scaling.py:194` binds `client = CLIENT_DICT[args.model]` — the dict's values are *classes* (`utils/clients.py:159-166`), and the sibling `induce_memory.py:139` instantiates with `(model_name=args.model)` where this one does not. `one_step_chat` carries no `staticmethod`/`classmethod` decorator (`utils/clients.py:149`), so `client.one_step_chat(trajectories, system_msg=…, temperature=0.7)` binds `trajectories` to `self` and leaves the required `text` parameter unfilled: **`TypeError` at the call, before any model is contacted.** Three further faults sit behind that crash, none of which could ever have been observed: (a) the N trajectories it assembles are N reads of **one** directory — `res_dir` and `cur_task` are loop-invariant inside `for i in range(num_samples)` (`:169-171`) and the caller passes a single `results_{i}` from *outside* its spawn loop, `i` being the leftover binding of the last trial (`pipeline_scaling.py:43,60,73`) — so `**Trajectory 1..N :**` are byte-identical and the self-contrast the prompt requests has nothing to contrast; (b) the `tuple[str, …]` return is stored without unpacking, where `induce_memory.py:164` unpacks it; (c) the bank entry stores the value bare (`:202`) where `induce_memory.py:184` stores `.split("\n\n")`, the list shape the consumer `run.py:189-191` iterates. |
| **Our handling** | `on_scaled_task_end` is written against the **paper plus the `PARALLEL_SI` text**, and now says so at the code site instead of citing `induce_scaling.py` as the thing it reproduces. This is the one place in this project where the standing rule *"reproduce it as shipped"* (see Cross-cutting patterns §2) has no available referent: a crash cannot be an arm. The deviation this forces is named rather than hidden — our hook receives **genuinely distinct** attempts, which the released code never did. The dead-and-inverted correctness label inside the same function is still reproduced as dead (B-7). |
| **Proof** | `scripts/repro/defects/repro_reasoningbank_matts_inert.py` — five assertions against the pinned clone: the unbound-call crash (static asymmetry against `induce_memory.py`, plus a dynamic half that rebuilds the pinned `one_step_chat` signature and makes the pinned call, catching the `TypeError` with no upstream dependency imported), the loop-invariant read, the unpacked/unsplit bank entry, the inverted label, and `SEQUENTIAL`'s zero references. $0, no model call. |
| **Impact** | Both MaTTS figures — the paper's scaling contribution — are unreproducible from the public repository, for two independent reasons at once. Stated with the same care as A-3: this is **not** a claim that 55.1 or 54.5 is wrong. It is a claim that the released artifact cannot produce them, which additionally means the released `induce_scaling.py` is **not the code that made them** — the trajectory-duplication bug alone would have been visible on the first successful run, so no successful run occurred. Anyone citing MaTTS as reproducible open-source work is citing something that does not exist yet. **The CLI surface corroborates this independently**: even the sibling that *does* run rejects half its own advertised `--model` choices — `"gpt-3.5"` is not a `CLIENT_DICT` key (`KeyError`) and `"gpt-4o"` resolves to the vision client, which takes no `temperature` and requires an `image` (`TypeError`) — and the paper's third backbone, Claude-3.7-Sonnet, is present in `CLIENT_DICT` but absent from both choices lists, so it cannot be selected at all (catalog RB-11, proven by the same Tier-0 script). |

---

## Tier B — paper ≠ code

### B-1 · MemoryOS: lowest-heat eviction exists in neither codebase

| | |
|---|---|
| **Paper claim** | §3.3: "segments with the lowest heat are evicted." Heat is the paper's headline mid-term memory mechanism, combining interaction count, page count and recency. |
| **Official code** | Both code lineages evict by **access-count LFU** — `mid_term.py:71-75` in the pypi core and `eval/mid_term_memory.py:56-60` in the copy that produced the paper's LoCoMo numbers. No heat-based eviction exists anywhere in either; the heat heap is used only for the promotion peek. The recency axis of heat is separately inert in the eval lineage: `R_recency` is a stored value (initialized 1.0, refreshed only on a retrieval hit) multiplied by γ = 1e-4 against β = 0.8, i.e. **1/8000 of a single interaction**, and the refresh recomputes decay against a just-updated timestamp so it always returns 1.0. The paper's own recency time constant is μ ≈ 1e7 s (≈ 2778 h); the code uses τ = 24 h. |
| **Our handling** | Relabeled `eviction="lfu"` in both presets, with `"lowest_heat"` retained as an explicitly non-lineage option, and the DELETE reason on the op log derives from the policy that actually ran rather than from the paper's vocabulary. The dead recency term is **reproduced deliberately**, because it is the published operating point, not fixed. |
| **Proof** | Catalog §4 (MOS-1, MOS-4, MOS-14), re-verified in round 12 with the verdict "HOLDS". `tests/test_organizers_phase3.py:249` (`test_memoryos_eval_lineage_recency_cannot_change_a_comparison`) asserts the heat difference between two segments differing **only** in recency is 1e-4 — under 1/1000 of one page's contribution — so the axis provably cannot change any comparison. |
| **Impact** | A paper headline mechanism exists in neither codebase, and a second one exists but cannot affect an ordering. This is the same family as A-MAC's constant novelty and recency: a feature presented as an axis of the score that is arithmetically incapable of moving it. |

### B-2 · G-Memory: the "0.7 similarity" edge gate is an effective cosine of 0.85

| | |
|---|---|
| **Paper claim** | Tasks are linked into the query graph when their similarity is at least 0.7; the resulting graph is what Eq. (5) neighbour expansion and Eq. (6) recall consume. |
| **Official code** | The Chroma collection is constructed with **no `collection_metadata`** (`GMemory.py:38-41`), so the space is Chroma's default `l2` — *squared* L2. `similarity_search_with_score` then yields `similarity = 1 − distance`, and with the harness's normalizing MiniLM embedder `d = 2 − 2·cos`, so the gate `1 − d ≥ 0.7` is exactly `cos ≥ 0.85`. Verified numerically rather than assumed: chromadb 1.5.9's default `l2` is squared distance, `cos 0.85 → 1 − d = 0.700` exactly, and the embedder's normalization was confirmed from the cached `modules.json` `2_Normalize` entry without loading the model. |
| **Our handling** | We gate **true cosine at 0.85**, with the L2 derivation written out in a comment at the gate, and removed the misleading `0.7` literal from the code entirely (commit `6fad7bb`). |
| **Proof** | `scripts/repro/defects/repro_gmemory_threshold.py`. It re-asserts the gate's shape and the metadata-free Chroma constructor in the pinned clone — so a later upstream commit that pins a space will fail the script rather than silently invalidate the derivation — then sweeps `cos` over `[−1, 1]` at 1e-3 resolution showing the two predicates are identical, and checks that our own gate encodes the derived constant. |
| **Impact** | Every published G-Memory graph's density is set by a mis-specified distance space, and the graph is the input to everything the method's equations do. |

### B-3 · Nemori: the 0.85 merge threshold is plumbed into a field nothing reads

| | |
|---|---|
| **Paper claim** | Merge candidates are filtered by similarity before the merge decision, at a threshold of 0.85. |
| **Official code** | `merger._similarity_threshold` is written once and never read. `_find_similar` (`merger.py:69-80`) hands the top-5 Qdrant hits straight to the merge-decision LLM with **no similarity filter at all**, and `config.py:86` faithfully plumbs the configured value into the dead field via `factory.py:55`. It is one of a cluster: `buffer_size_max` (`config.py:73`), `episode_max_messages` (`:77`) and `semantic_similarity_threshold` (`:82`) are likewise never read, which makes the eval config's `buffer_size_max: 20` inert as well. |
| **Our handling** | Removed on our side too. Our `upstream` preset had **resurrected** the dead knob as a live 0.85 filter, which suppressed most merge-decision LLM calls and would have made our "upstream" runs cheaper than upstream. Round 12 caught it; `merge_similarity → None` for that preset (commit `688c959`), and the knob survives only as a documented extension of *ours*. |
| **Proof** | `scripts/repro/defects/repro_nemori_dead_knob.py`: an AST walk asserting exactly one `Store` and **zero** `Load`s of the attribute, plus the factory line that plumbs the config value into it. |
| **Impact** | No effect on upstream's published numbers — the filter was never active when they were produced. The entry is here because of the second half: this is the defect class that produced a real error on *our* side, and it is why the "dead knobs stay dead" rule below is a standing project stance rather than an observation. |
| **Measured (2026-08-03)** | Both arms run on LoCoMo (10 conversations, gpt-4o-mini, `all-MiniLM-L6-v2`, judge = Mem0-J binary at gpt-4o-mini, `keyword_queries=False` — Nemori's zero-LLM read path; artifacts `results/repro/gpt-4o-mini_nemori_{upstream,merge085}_*`; note: seed-1 *ingest* stamps predate the keyword_queries fix and record `True` — the field is read-path-only and inert at ingest; every eval stamp records `False`). The defect-faithful arm (filter dead, as shipped) spends **+807 write calls (+29%, merge-stage calls (decision + synthesis) 961 vs 176)** and scores **lower**: J 63.38 vs 65.52 (−2.14 pp), F1 42.66 vs 44.91, with the largest gap on temporal (J 52.65 vs 58.25). The paper's *intended* 0.85 filter is strictly better than the code it shipped in both cost and quality on this run. Merge-candidate similarity distribution captured per conversation (committed summaries `results/repro/nemori_merge_score_summary.json` for MiniLM, `..._e3s.json` for `text-embedding-3-small`). **Correction (2026-08-05): the "16.0% of 8,083 candidates clear 0.85" figure previously quoted here pooled BOTH arms and should not be cited.** Split by arm, where the two differ by 3.6x and for a reason: arm A **6.6%** of 3,668, arm B **23.9%** of 4,415. Arm A's filter is dead, so merges proceed freely and the surviving episodes are more distinct from one another; arm B blocks most merges, so near-duplicate episodes survive and later candidates score higher against them. Pooling two populations that the arm itself creates produced a number describing neither. **The threshold does not port across embedders.** Same arm, same ten conversations, only the embedder swapped: arm B clears 0.85 on **23.9% of 4,415 candidates under MiniLM but 4.1% of 4,536 under `text-embedding-3-small`** — a 5.8x swing in what a hardcoded constant filters, with the candidate populations nearly identical in size (4,415 vs 4,536), so the swing is the embedder's similarity scale rather than a different set of episodes. Upstream tuned 0.85 against `gemini-embedding-001` and documents no embedder dependence; on a different one the same literal is silently a different filter. It does not become vacuous — 188 candidates still clear it and merge-stage decision calls stay non-trivial (176 -> 130) — so arm B remains a valid arm, which is the specific risk the Track 1 precheck raised. Write-path scale check: our upstream-faithful arm averages 356.9 calls/conversation vs upstream Table 3's 373.2 (−4.4%). Same-protocol J for the upstream-faithful arm lands 9.6 pp below the paper's 73.0 LoCoMo headline and below Full-context 72.90 — **which is Mem0's published Table 2 figure (arXiv:2504.19413, verified 2026-07-23), not ours: no LoCoMo full-context run of ours exists in results/, and the earlier wording "our measured" here was a misattribution (corrected 2026-08-19; caught independently twice while writing the X4/X6 notes)**. The embedder was the named confound (upstream used `gemini-embedding-001`; we keep the campaign-standard `all-MiniLM-L6-v2` for cross-methodology comparability) — and it is now **measured rather than named**. Embedder diagnostic (2026-08-04, single seed, arm A only, same harness/judge/read-k, only the embedder swapped to `text-embedding-3-small`; artifacts `results/repro/gpt-4o-mini_nemori_upstream_*_e3sA*`): **J 63.38 → 67.60 (+4.22 pp), F1 42.66 → 46.79, BLEU-1 37.82 → 41.74**, so the gap to the paper narrows 9.62 → 5.40 pp. Two conclusions, and the second matters as much as the first: the embedder accounts for **roughly 44% of the shortfall**, and **the remaining 5.40 pp is not the embedder** — it stays unexplained, so no write-up may attribute the whole gap to it. The mechanism is read-side: the ingest output barely moved (semantic 1,881 → 1,926, +2.4%; episodes 777 → 778, +0.1%, with per-conversation signs mixed 5↑/4↓/1=), meaning the same memories were stored and simply retrieved better at 1,536 dims. Consistent with that, the gain concentrates in retrieval-sensitive categories (multi-hop +5.68, single-hop +4.28, temporal +3.74) and all but vanishes on open-domain (+1.04), where the answer is not in memory to be found. Single seed, but Track 1's seed-2 replication puts per-arm J stability at ±0.35 pp, so +4.22 pp sits well outside replication noise. Embedding spend was negligible (879,628 tokens, $0.018), so the substitution's cost objection is not a real one at this scale. Seed-2 replication (2026-08-03): the quality gap holds in direction on all three metrics (ΔJ +2.14 → +1.62 pp), and per-arm J is stable to ±0.35 pp. Model axis (gpt-5.6-luna, fixed sampling — temps not honorable, disclosed in the stamp; judge unchanged at gpt-4o-mini): the **quality** damage vanishes on the stronger model (ΔJ −0.32 pp, within noise) while the **cost** damage stays structural (merge-filter arm still −19.8% write calls; luna also segments finer — 84 episodes/conv0 vs 52 — so write-path cost is model-dependent beyond token prices). **Embedder axis (2026-08-05, single seed, both arms re-ingested and re-evaluated on `text-embedding-3-small`, everything else identical; artifacts `..._{e3sA,e3sB}`): the quality conclusion REVERSES, the cost conclusion does not.** On MiniLM the intended filter wins (J 65.52 vs 63.38, **+2.14 pp** for arm B). On the hosted embedder the shipped defect wins (J 67.60 vs 65.78, **−1.82 pp** for arm B) — and F1 and BLEU-1 flip with it (+2.25/+2.07 becomes −1.00/−1.06), so it is not one metric wobbling. Write-path cost is unmoved: arm A spends **+29.2%** more calls on MiniLM and **+29.7%** on the hosted embedder, merge-stage 961→176 and 974→130. The mechanism is the pass rate measured above: arm A's dead filter sends EVERY candidate to the LLM merge decision, so its merges are only as good as the similarity signal. Under MiniLM that signal is unreliable and the filter earns its keep by blocking bad merges; under `text-embedding-3-small` the signal is reliable while the same 0.85 constant now admits just 4.1%, so the filter blocks good merges instead. Corollary worth its own line: the embedder upgrade is worth **+4.22 pp to arm A but only +0.26 pp to arm B** (65.52 → 65.78), because an arm that has stopped merging converges toward "merge off" and cannot spend a better similarity signal. **Single seed on this axis; the reversal is larger than Track 1's measured ±0.35 pp per-arm seed stability but a sign flip deserves replication before it is quoted as settled.** One sentence of the finding, restated across all three axes now measured: *the dead knob always costs money — about 30% of the write path — while whether it also costs quality depends on the model answering and on the embedder retrieving, and on a good embedder the shipped defect is the better of the two.* |

### B-4 · Zep/Graphiti: the code runs ahead of its paper, and the mentions reranker sorts backwards

| | |
|---|---|
| **Paper claim** | §2.2 describes separate extraction stages, a reflexion-style entity recheck, fulltext candidate collection, and a dedicated temporal-extraction pass; and it argues that frequently referenced items become more accessible over time. Paper v1 (2025-01-20) is the only version. |
| **Official code** | Drift runs in both directions. Absent from current main though claimed by the paper: the reflexion recheck (`grep reflexion` returns 0 hits), fulltext candidate collection (replaced by cosine top-15 at ≥ 0.6 plus MinHash/LSH), and the separate `extract_edge_dates` prompt with its `temporal_operations.py` (dissolved into `extract_edges`). Absent from the paper though present in main: saga nodes, the entity-attribute ontology, and **combined single-call extraction**, which runs directly against §2.2's separate-stage design. Separately, `episode_mentions_reranker` **sorts ascending** (`search_utils.py:1860-1896`, `sorted_uuids.sort(key=scores)` with missing nodes scored `inf`), so the most-mentioned items rank **last** — the exact inverse of the paper's accessibility argument, and almost certainly an upstream bug. |
| **Our handling** | The lineage is pinned rather than blended: we implement separate-stage extraction (the paper side) and track the additions as additions. For the reranker we follow the **paper** (descending), and the deliberate choice of paper over upstream is named in the docstring instead of being silently right. |
| **Proof** | Catalog §5 (ZEP-1, ZEP-6); round 12 `# [zep]` #10, confirmed by reproducing the inversion numerically in the verification round. |
| **Measured (2026-08-08)** | The reranker is now measured end to end, in the paper's direction, and **the paper's direction loses**. Two LoCoMo arms against one identical ingest, differing only in the reranker — `edge_rrf` (facts only, no reranker) vs `edge_episode_mentions` (facts only, mention count descending): **J 34.87 vs 33.05**. Paired percentile bootstrap over the 1,540 per-question verdicts (10,000 resamples, seed 0): **ΔJ +1.82 pp, 95% CI [+0.26, +3.38], p = 0.024**, 150 questions disagreeing — the only separated pair in a five-recipe sweep whose other two comparisons both cover zero. So reranking facts by how often episodes mention them is **worse than not reordering at all**, at the one significance level this benchmark can support. Two readings are open and the campaign does not separate them (mention count is a popularity prior that outranks relevance; or LoCoMo asks disproportionately about things said once). Artifacts `results/repro/gpt-4o-mini_zep_edge_{rrf,episode_mentions}_all_k10_*`, bootstrap `results/repro/zep_recipe_power.json`, write-up `docs/18-locomo-4way.md`. |
| **Impact** | Zep is the sole reverse case in this ledger — a project whose code is *ahead* of its paper rather than behind it — which is why any "Zep fidelity" statement is meaningless unless it says paper-or-main. The reranker inversion contradicts a stated paper mechanism directly — and now that the mechanism has been measured, the choice of lineage has a number attached: the paper's direction is worse than no reranker at all, so "follow the paper here" cost us 1.82 J and we would make the same choice again, because the alternative was to reproduce an accident silently. **This does not vindicate upstream's ascending sort.** That order was not run; ascending is the reverse ranking, not the absence of one, and the sweep says only that ordering facts by mention count in the direction the paper argues for is harmful. |

Two further Graphiti findings are worth a line each because they sit at the boundary of "bug" and "specification". Upstream's label propagation for community detection can **never terminate** on a common input: synchronous relabelling inside `while True` with no round bound means a two-node component joined by *two* edges swaps labels forever, and a two-entity, two-fact subgraph is an ordinary dialogue shape. We added 2-cycle detection and a `max_rounds` bound, both warned — and we did **not** invent a merge, because in every state of that cycle the two labels differ, so the honest reading of the published rule is that it cannot cluster that component. And upstream's RRF uses `rank_const = 1` (`search_utils.py:1780-1786`), not the textbook `1/(60+rank)`; across multiple channels that is not a monotone rescale, since items at ranks (1,100) and (3,3) order differently under the two constants. Same function name, different math — so `rrf_k` is configuration in our port (default 60, Zep recipe 1).

### B-5 · A-MAC: θ\* was selected on the evaluation data, and the weights are not in the paper

| | |
|---|---|
| **Paper claim** | Weights and threshold are chosen by 5-fold cross-validation over a grid; Table 2 reports the features' relative importance. |
| **Official code** | The cross-validation never fits anything. `optimize_weights_cv.py:180-185` assigns `X_train, y_train` and then calls only `evaluate_weights_threshold(X_val, y_val, …)`; selection at `:198-201` reads the best result off that evaluation, and `final_result` at `:204` re-scores on the full data — so weights and θ are chosen **on the data they are then reported against**. The candidate set is 1 equal-weight vector + 100 random + 5 feature-emphasized = 106 vectors, not the paper's grid; only the θ list `[0.3…0.6]` matches. The separate `weight_optimizer.py` never fits either (`val_data = train_data` when None, `:69-70`) and passes `candidate.dialogue_turn`, which exposes `.text`, into extractors expecting `.content` (`:110-113`), with `existing_memories=[]` again at `:113`. The operating weight vector `[0.1,0.1,0.1,0.1,0.6]` **is not in the paper at all** — it exists only in the repo's `README.md:79-80` and `results/optimized_weights_cv.json`, while the code defaults in `scorer.py` are equal weights `[0.2]*5` at θ = 0.5. That README quick-start is itself not runnable against the release's own classes: it imports a nonexistent `WeightOptimizerCV`, constructs `MemoryCandidate(turn=…)` where the real field is `dialogue_turn`, and calls `scorer.score(candidate)` where `score` takes a `Features`. |
| **Our handling** | We do not ship their fitting code, so there is nothing to fix; the finding is used as the documented reason the release operating point is **not transferable** to debugged features. All our prose says "the release's weights", never "the published weights" (corrected at three sites), and the operating point is kept as our default with a re-tuning warning at the config site. |
| **Proof** | Catalog §1 (A-MAC-5, A-MAC-8); round 12 `# [amac]` #3 and #5–#7, corroborated by a third independent pass recorded in `docs/16-abstraction-study.md`. |
| **Impact** | Combined with A-1, this is why the A-MAC ablation planned in this repo must **re-tune** before measuring: an operating point fitted on the evaluation data, on top of a feature set where two of five features are constant and a third is driven by a substring bug, has no reason to survive the removal of any of those conditions. |

### B-6 · ACE: the "refine" half of grow-and-refine is off in the published runs

| | |
|---|---|
| **Paper claim** | Grow-and-refine: the playbook grows by adding bullets and is refined by merging and deleting them, with refinement triggered lazily once the context budget is exceeded. |
| **Official code** | The curator's apply path supports **ADD only**, and says so in its own comment — *"Currently only ADD operations are fully supported. Note: You can add support for UPDATE, MERGE, DELETE operations here"* (`ace/core/curator.py:212-213`). The failure mode is quieter than that comment implies: validation at `curator.py:214` warns only for operation types *outside* `["ADD","UPDATE","MERGE","DELETE","CREATE_META"]`, so an emitted UPDATE, MERGE or DELETE **passes validation with no warning at all** — and `apply_curator_operations` then handles only `ADD` (`playbook_utils.py:143`), its UPDATE/MERGE/CREATE_META branches sitting commented out at `:131-141` beneath a docstring that lists them as *"Future Operations (not implemented yet)"* (`:100-104`). Non-ADD operations are therefore **silently discarded** — no warning, no effect, no record. The refine half lives in a separate component that is **opt-in, off by default, and fails silently**: `use_bulletpoint_analyzer=False` (`ace/ace.py:41,133`), the eval harnesses expose it as a `store_true` flag defaulting to off (`eval/finance/run.py:83`), the README documents the default as false, and when `sentence-transformers`/`faiss` are absent it returns the playbook **unchanged after a warning** (`bulletpoint_analyzer.py:290-292`). The paper's *lazy* trigger is implemented nowhere — only the proactive one — and the code default `max_num_rounds=3` (`ace.py:123`) sits against the paper's up-to-5 refinement ablation. |
| **Our handling** | Our ADD-only curator is therefore **equivalent to upstream**, not a simplification of it, and the module docstring says exactly that. We run dedup always-on at threshold 0.90 — but round 12 forced an honest refinement of how we described it: ours is a **third** behavior, not upstream's analyzer switched on. Upstream-enabled dedup compares the *whole* playbook including old-old pairs and LLM-merges groups with summed helpful/harmful counters (`bulletpoint_analyzer.py:193-336`, `ace.py:600-606`); ours drops the incoming bullet. Both are disclosed at the code site. |
| **Proof** | Catalog §7 (ACE-1 through ACE-4); round 12 `# [ace-rb]` #1. No Tier-0 script — this entry rests on code-line citation plus the verification verdict alone. |
| **Measured (2026-08-17, the retry arm — and the finding that removes an explanation)** | The last open confound in this entry was D4: upstream reflects, re-answers from that reflection, and reflects again, up to `max_num_rounds`=3 with early stop (`ace.py:501-543`), where ours reflected once. That loop is now reproduced (`ACEOrganizer.max_rounds` + `finer.retry_generator`) and run over the same 441 samples at the same dedup: **retry tag 45.80 / sample 12.70, 2,918 calls, $4.394**. Paired: **retry − online Δ −2.72 pp, CI [−5.67, +0.00], p = 0.067 — not separated**; retry − base Δ −3.40 pp, CI [−6.12, −0.68], p = 0.021, which is below 0.05 and above the 0.0125 a Bonferroni correction for four comparisons requires. **Upstream's extra calls bought no measurable accuracy at 2.2x the cost of the single-reflection arm.** What they DID buy is the thing this campaign had named as the cause of the null, and that is why this row matters more than its interval: the earlier rounds diagnosed a *capture* failure — the task was repetitive, the errors concentrated, and the playbook did not cover them (7 of the top 50 confusions named, 9.4% of proposed bullets carrying a US-GAAP identifier). Under retries the curator writes **30 of the top 50** confusions, names **73** of the 115 gold tags ever missed, and its proposal specificity **rises across the run — 11.8 / 47.3 / 67.3 / 68.2 / 69.1% by fifth** — because 385 of 441 samples were re-answered and **129 were corrected inside the training step**, so the reflection handed to the curator describes an attempt a specific correction fixed. **The knowledge was captured, specifically and increasingly, and accuracy fell.** The capture explanation for ACE's null on FiNER is therefore excluded at this operating point; what remains open, and is NOT separated by this run, is whether serving 217 specific rules on every question costs more than the rules are worth (the per-window record is consistent with it: +1.00 pp against online over the first half, −3.28 pp over the second) or whether knowledge derived from a corrected retry is unreachable on a first attempt. Single seed. Write-up `docs/19-ace-finer.md`; artifact `results/repro/gpt-4o-mini_ace_finer_retry.json`, whose records carry `retry_attempts` and `corrected_in_training` per sample. |
| **Measured (2026-08-14, the arm that tests this entry's own subject)** | The row below left one confound standing: our dedup is a deviation from upstream's shipped default, so a null under it could not distinguish "the adaptation does not transfer" from "our gate threw the adaptation away". A third arm now runs at **upstream's default — dedup off** — over the same 441 samples: **nodedup tag 48.98 / sample 17.01 at $8.633**. Paired against base: **sample_accuracy Δ +0.91 pp, 95% CI [−2.04, +3.86], p = 0.607 — not separated**; tag Δ +0.74 pp; ahead in 14 of 30 windows, behind in 14. Against the online arm: Δ +1.59 pp, CI [−1.59, +4.76] — also not separated. **Our dedup was not the reason for the null.** Two measurements came with it. **Cost is carried in tokens, not calls**: turning the gate off multiplied cost by 5.9 while the call count moved from 1,323 to 1,325, because the playbook is injected whole into every generator and curator call — by the end of the run each call carried ~117 K tokens of playbook — the final generator call billed **118,619 input tokens, 93% of `gpt-4o-mini`'s 128 K context**, 98.6% of it playbook — around a 1,951-token task (corrected 2026-08-17 from an unverified "~95 K / 74%"; the trace says the last window's generator calls billed 110,646–118,692, so the earlier figure understated the finding). And **the bigger playbook holds less knowledge**: 2,165 bullets against 140, yet it names both tags of **0** of the top-50 confusions where the small one names 7, and carries a CamelCase identifier in 4.0% of bullets against 17.1%. The trace separates the two causes — the curator's *proposed* bullets drift to abstraction as the playbook grows (specificity by fifth of the run: 2.3 / 15.9 / 1.8 / **0.0 / 0.0**, so ~866 consecutive bullets name no US-GAAP element), while our gate independently *selected* for specificity (online proposals 9.4% → survivors 17.1%). Head errors did fall (top-8 confusions 183 → 95) with total wrong slots nearly unchanged (913 → 900): the errors moved into the tail rather than disappearing. **Call counts here are generator-plus-curator calls read off each arm's trace** (441 / 1,323 / 1,325) — the row below says 3,775 for the same online arm because it sums the run summary's `llm_budget`, which also counts the 2,452 embedder calls the curator's dedup makes; neither number is wrong and both must say which they are. The summary's own budget cannot be used for a resumed arm at all: it counts only the last process (164 calls of 1,325), while `cost_usd` in the same file is the measurement total. Single seed. Write-up `docs/19-ace-finer.md`; artifact `results/repro/gpt-4o-mini_ace_finer_nodedup.json`. |
| **Measured (2026-08-10)** | ACE's online adaptation is now measured on FiNER — the benchmark its README states the online claims in, and the only ACE experiment whose data ships. Two arms, the same 441 samples (1,764 tag decisions) in the same order at `gpt-4o-mini`, differing only in whether a playbook was grown alongside: **base (no learning) tag 48.24 / sample 16.10 at 441 calls and $0.192; online tag 46.71 / sample 15.42 at 3,775 calls and $1.461 (7.6x)**. Paired percentile bootstrap over the same questions (10,000 resamples, seed 0, statistics imported from `scripts/ext/x1_power.py`): **sample_accuracy Δ −0.68 pp, 95% CI [−3.40, +2.04], p = 0.687 — not separated**; tag_accuracy Δ −1.53 pp (point estimate; the published metric is a clustered per-question rate the boolean bootstrap cannot take, and no second CI implementation was written for it). Online is ahead in 10 of 30 windows, behind in 16. **Growing the playbook bought no measurable improvement at 7.6x the cost** — and the point estimates being negative does NOT support "the playbook hurts", since the interval covers zero. The playbook was real: 140 bullets survived 0.90 dedup out of 441 curator calls, 43,699 characters, injected whole into every generator call, with specific content ("Differentiate between `AmortizationOfFinancingCosts` and `AmortizationOfIntangibleAssets`"). Not a reproduction of 69.1 → 81.9: our generator is 20.9 points under the paper's baseline (`gpt-4o-mini` vs DeepSeek-V3.1) and our adaptation loop is 3 fixed calls per sample against upstream's **5–10** (corrected 2026-08-15 from "3–8", which counted only the retry block: upstream also re-answers each sample inside `_train_step` at `ace.py:465-472` and again after curating at `ace.py:608-625`, neither of which feeds learning). **That confound was closed on 2026-08-17 — see the retry row above.** Single seed. Write-up `docs/19-ace-finer.md`; artifacts `results/repro/gpt-4o-mini_ace_finer_{base,online}.json`, analysis `results/repro/finer_paired.json`. |
| **Our handling (correction, 2026-08-10)** | This entry previously said our ADD-only curator is "equivalent to upstream, not a simplification of it". That was true of the *operations* and false of the *prompts*, which is what actually decides what a bullet contains. Two upstream sentences had been paraphrased away: `REFLECTOR_PROMPT` says the subject is "a **model's** reasoning" and asks what "**the model** should have done instead" (prompts/reflector.py:6-18) where ours said "critique this task execution"; and `CURATOR_PROMPT`'s Context block (prompts/curator.py:7-9) states that the playbook will be used to answer similar questions and that the ground truth the reflection was written against will **not** be available then — ours dropped it entirely. With both gone the reflector personified a human operator ("the user may have overlooked…") and the curator wrote advice for that operator: in a 30-sample paid smoke, **every** bullet was process improvement ("establish a periodic training programme", "pair less experienced users with seasoned professionals"). Restored at `6783c24`, with the measurement recorded at the code site; the same 30 samples then produced tagging knowledge. No published number moved — ACE had never been run for a result — but the Measured row above would otherwise have been a measurement of our prompt wearing ACE's name. Found by reading a paid smoke's trace before spending on the full run, which is the argument for the smoke step. |
| **Impact** | The shipped reproduction path **defaults to no-dedup** — off in `ace.py`, off in the eval harness flag, off again by silent fallback when the optional dependencies are missing — so on the released code the refine axis of the paper's central mechanism is inactive unless a runner turns it on deliberately. We do not claim to know which setting produced any specific published figure. A separate reproducibility limit sits beside it: the AppWorld experiment code — the paper's headline benchmark — is absent from the public repository, leaving only FiNER/Formula and mind2web publicly runnable. |

### B-7 · ReasoningBank: the MaTTS correctness label is dead, and inverted

| | |
|---|---|
| **Paper claim** | MaTTS parallel induction distinguishes successful from failed trajectories, feeding a per-trajectory correctness signal into memory induction. |
| **Official code** | The label is computed and then never rendered. `induce_scaling.main()` derives each trajectory's `status` and passes it to `get_info`, but `format_examples` — the helper that builds the `## Correctness Signal` block — is **never called from `main()`**, so nothing reads the field. The only signal that actually reaches the prompt is the mixture itself ("Some trajectories may be successful, and others may have failed"). The same dead field is additionally **inverted**: `induce_scaling.py:181-184` sets `status="success"` when `reward == 0`, the opposite of the single-trajectory path at `induce_memory.py:131-134` — so it would be wrong if it were live. |
| **Our handling** | Reproduced as dead. `on_scaled_task_end` deliberately takes **no** per-trajectory outcome argument, because adding one would invent a channel the released mechanism does not have; items are stored as `outcome="contrast"`. The inversion is noted in one line of our docstring as further evidence of the deadness. |
| **Proof** | `scripts/repro/defects/repro_reasoningbank_matts_inert.py` (`prove_inverted_label`) asserts both literals against the pinned clone. Catalog §8 (RB-1, RB-2); round 10 §R1, round 12 `# [ace-rb]` #12. |
| **Impact** | Deliberately modest: a labelled-supervision claim in the paper is inert in the released implementation. No published number is shown to move as a result, and we do not claim one does — this is a latent finding that constrains how the mechanism may be described, not evidence against a result. **Read with A-5**: the function this label lives in does not run at all, which makes the deadness over-determined and explains how an inverted comparison survived review — it was never executed. |

### B-8 · A-Mem: the read path the paper describes is not the read path that made the numbers, and the extra step costs 5.26 J

| | |
|---|---|
| **Paper claim** | Retrieval is dense top-k over the notes plus a one-hop link expansion. The paper's contribution is entirely on the write path; the read path is presented as plain similarity search, with **no LLM call**. |
| **Official code** | The evaluation harness calls one first. `answer_question` opens with `keywords = self.generate_query_llm(question)` and then searches with *those keywords instead of the question* — `raw_context = self.retrieve_memory(keywords, k=self.retrieve_k)` (`test_advanced.py:129,134`; identically in the robust copy at `test_advanced_robust.py:111-112`). The prompt is *"Given the following question, generate several keywords, using 'cosmos' as the separator"* (`test_advanced.py:96-103`). So every question costs an extra LLM call, and the embedding search never sees the question. A second read-side LLM step exists — `retrieve_memory_llm`, which would ask the model to select the relevant parts of the retrieved context — but it is **commented out** at both call sites (`:137`), so the live read path is exactly one LLM call. |
| **Our handling** | Reproduced, not corrected: `keyword_queries=True` is the default in `scripts/repro/configs.py`, which is why every A-Mem number in this repo carries it. The alternative is wired alongside it as `amem_rawq` — same organizer, memory types, temperatures and store, differing at retrieval alone, pinned by `tests/test_repro_artifacts.py::test_amem_rawq_differs_from_amem_at_retrieval_only`. The four-way table's headline A-Mem row stays on the faithful (keyword) path, with the ablation shown beside it. |
| **Proof** | The pinned clone's two harness copies for the code half; for the measurement, a pair of committed eval summaries — `gpt-4o-mini_all_k10_ours_expand-on_run1_e3s.json` (keywords) and `gpt-4o-mini_amem_rawq_all_k10_ours_expand-on_run1_e3sRAWQ.json` (raw question) — whose stamps differ in `config` and `keyword_queries` and in nothing else (model, judge, embedder, k, expand_links, workers, temperatures, dataset hash all identical). |
| **Measured (2026-08-06)** | Dropping the rewrite and searching the raw question moves **J 59.87 → 65.13 (+5.26)**, F1 41.41 → 46.21, BLEU-1 36.51 → 40.86, while *removing* 1,986 LLM calls and $0.037. The mechanism is read-side coverage: abstention falls 26.9% → 21.6% with accuracy-when-answered flat (82.0% → 83.0%), and the retrieved sets genuinely differ (top-1 identical on 65.3% of questions, mean Jaccard 0.598). Gains concentrate where retrieval decides the outcome — single-hop +6.30, multi-hop +6.03 — and are smallest on open-domain (+3.12), the same signature the embedder diagnostic produced. Measured at the global-5 link budget; at the faithful per-hit budget of C-7 the same change is worth **+4.35** (61.23 → 65.58), because the two overlap — see C-7's interaction note. Single seed, one embedder. |
| **Impact** | The published system spends an LLM call per question to score **worse** than the retrieval its own paper describes. Stated carefully: this is measured under our harness at `gpt-4o-mini` + `text-embedding-3-small`, and a keyword query may well have been the better choice for the weaker embedders of the paper's era — the step is not obviously wrong, it is *unmeasured upstream and no longer earning its cost*. For anyone deploying A-Mem the actionable form is simple: the rewrite is a knob, and turning it off was worth +5.26 J and −1,986 calls here. |

**Correction to our own earlier claim, made the same day it was published.** This entry first landed as **C-6**, framed as a defect of *our* harness — "our own read path was costing A-Mem 5.26 J points" — on the strength of this repo's own `13-amem-study.md`, which listed the keyword rewrite as one of two deviations *we* had introduced. Re-reading the pinned clone showed the opposite: the rewrite is upstream's own eval behavior, present in both harness copies, and our configuration was faithful. The study document had summarized the *paper's* description of the read path and then reasoned about it as though it described the *code* — the mirror image of the repo's own class-(a′) failure, and a reminder that the standing rule ("the code that produced the numbers is a different question from what the code does") applies to our own documents too. The measurement was never in doubt; only the direction of the arrow was. C-6 is withdrawn; `13-amem-study.md` §1 and §6-2 are corrected at the source.

---

## Tier C — the evaluation harness itself

### C-1 · LoCoMo category 5 is scored as a two-choice MCQ with the gold answer displayed

| | |
|---|---|
| **Paper claim** | The adversarial category measures whether a system correctly declines to answer questions the conversation does not support; A-Mem reports 50.03 on it. |
| **Official code** | The official evaluation presents the **gold answer and "Not mentioned in the conversation" as a two-choice multiple-choice question** (`test_advanced.py:146-160`), with a temperature of 0.5 dedicated to that category — the correct answer is in the prompt. Option order is decided by an **unseeded** `random.random() < 0.5` (`:149-154`), so reruns are not reproducible. Category 3's gold is a `"A; B; C"` string of which only the primary answer is graded, category 5's gold is `adversarial_answer`, and category 5 is scored with the same token F1 as every other category. |
| **Our handling** | We keep the MCQ for reproduction — removing it would produce a number that is not comparable to anything published — but derive the coin from `md5(question)` so ordering is byte-stable across reruns, and add an `(a)/(b)` → option-text resolver. Our default outside reproduction mode is the stricter non-MCQ prompt. |
| **Proof** | `docs/14-amem-reproduction.md` §2 transcribes the entire upstream scoring table with `file:line`, and its "cat5 MCQ reproducibility" section records our handling; the gold-leakage reading is corroborated externally by upstream issue #8. |
| **Impact** | The published adversarial score is measured under gold exposure, and no rerun of the official script reproduces its own option ordering. |

### C-2 · Three incompatible graders are all called "the A-Mem LoCoMo eval"

| | |
|---|---|
| **Paper claim** | LoCoMo F1 numbers across this literature are presented as mutually comparable. |
| **Official code** | They are computed by at least three different functions. `snap-research/locomo` uses a SQuAD-style `normalize_answer` — lowercase, punctuation strip, **article removal** (`a/an/the/and`) and **Porter stemming** — with a multiset F1. `WujiangXu`'s `utils.calculate_metrics` uses a **set-based** token F1 with lowercasing and `.,!?`→space, **no stemming and no article removal**, plus BLEU-1 on a different tokenizer entirely (`nltk.word_tokenize` with `sentence_bleu((1,0,0,0), SmoothingFunction().method1)`). Mem0's J-score is a binary LLM judge over categories 1–4 only. Stemming and article removal both *increase* partial credit, so mixing the three silently moves scores. Two smaller traps ride along: `snap-research`'s `rougel_score` returns a **rouge-1** F despite its name, and that repository contains no BLEU implementation at all; and neither official grader has a J-score, so every J number in this literature comes from Mem0's re-run protocol rather than from the original authors. |
| **Our handling** | Fully separated code paths — `--eval-mode ours\|wujiang` plus an opt-in `--judge` — so a stored result names the scorer that produced it. `bleu1_wujiang` calls real nltk and **omits** the metric when nltk is absent rather than reporting 0.0. Our own earlier `wujiang`-mode BLEU was a hybrid (upstream gold and F1 with our normalization); the affected stored artifacts are flagged as hybrid rather than quietly re-labeled. |
| **Proof** | `scripts/repro/defects/repro_locomo_rescoring.py`, run by CI, replays the eight committed `results/repro/*.records.jsonl` artifacts with no new spend. Replayed under the scorer lineage that produced each file, **12,314 questions** re-score to a maximum drift of **5.00e-04** — pure rounding, so any auditor can re-derive our stored headline numbers offline. Replayed across lineages, **2,804 of those 12,314 questions score differently**, and the count is pinned so a scorer edit fails CI. The asymmetry is the point: a *uniform* replay does not reproduce the stored values at all (uniform `token_f1` on the wujiang-scored files drifts by up to 1.0), which is itself the evidence that scorer lineage is load-bearing. To be precise about what CI guards: the script asserts the lineage-matched drift bound and pins both counts; the uniform-replay figure was measured out of band and is quoted here as context, not asserted by the script. |
| **Impact** | Every cross-paper LoCoMo comparison must name its grader, and A-Mem's LoCoMo score is in fact reported as 52.5, 61.4, 65.3 and 68.83 across three citing papers — a 16-point spread for one system on one benchmark — so only same-backbone, same-grader comparisons are defensible. |

One risk in this area was closed by measurement rather than by argument, and the result is negative: nltk's default `PorterStemmer` differs from Porter (1980) — the step-1c `y→i` condition differs, so `cry`/`cried` do not merge under the original algorithm — but replaying the same eight stored artifacts under both stemmer modes changed **zero** questions and moved F1 by 0.000. Residual divergence between the two implementations touches 10 of the 692 LoCoMo vocabulary words. We still pin the mode explicitly in `bench/locomo.py`, because a stored artifact should not depend on which library version was installed, but the correction is not a source of anyone's error.

### C-3 · MemoryOS batching: the cost comparison measured our deviation, not MemoryOS

| | |
|---|---|
| **Paper claim** | MemoryOS's batched write path is economical relative to per-message extraction methods — a claim **this repository's own earlier summary repeated**. |
| **Official code** | The upstream design spends, per page, one TOPIC call plus one continuity call plus one meta-info call, which is roughly **600+ LLM calls per conversation** — making MemoryOS-the-method *more* expensive than A-Mem or Nemori, not less. Our earlier "91 calls, cheapest by batch design" figure measured **our** batching, which had deviated from upstream's. Two further details make the accounting copy-dependent: the first page's continuity call is made against an empty previous page and its answer discarded (`utils.py:359-374`, `updater.py:130-132`), and the retrieval-stage keyword term is *dead* in the pypi core (`mid_term.py:292`, `query_keywords = set()` sitting directly above the term that consumes it) but **live in the eval lineage with a different formula and an extra LLM call per question** (`eval/mid_term_memory.py:200`) — and different again in the chromadb copy, which runs the query through the write-side prompt and scores with Jaccard. |
| **Our handling** | `docs/09-results-summary.md` conclusion 3 was formally corrected and the header carries the correction; the old wiring is preserved as an explicitly named config (`memoryos_mixed`) so the earlier artifacts remain reproducible rather than being orphaned; our skip of the first continuity call is documented as a known 1-call-per-conversation difference. |
| **Proof** | Catalog §4 (MOS-16, MOS-8, MOS-7), from round 8 §M1 and round 10 §M8. A regression test, `test_the_read_and_merge_keyword_formulas_are_the_same_function` in `tests/test_organizers_phase3.py:1352`, pins the read-time and merge-time keyword formulas to be the *same function per lineage*, which is the property the three upstream copies violate. |
| **Impact** | Any cost claim about MemoryOS must name the copy, because the lineage that produced the published numbers spends a read-time LLM call the other two do not. Listed here rather than buried because the corrected table was ours. |

### C-4 · LongMemEval: two different "accuracies", and a label that leaks the answer

| | |
|---|---|
| **Paper claim** | A LongMemEval accuracy, reported as a single comparable number — including a full-context baseline of 60.6. |
| **Official code** | The official aggregator prints **two** accuracies that are not equal and does not force a choice between them: `Task-averaged Accuracy` (the mean of six per-type means) and `Overall Accuracy` (over all questions), with unbalanced type counts (`print_qa_metrics.py:31-33`). Abstention is not a seventh type but a **cross-cutting slice** — each entry goes into its own type bucket and `_abs` entries are *additionally* added to the abstention bucket (`:19-23`). (**Corrected 2026-08-19**: this entry previously said those 30 questions were "double-counted inside both figures", and a re-read of the aggregator shows they are not — `all_acc` accumulates over the six type buckets only (`:28`) and `Task-averaged` averages those six means (`:29`), so no row is counted twice *within* either headline. The real hazards are two: the 30 abstention rows are **not excluded** from either headline, so a system that farms "I don't know" collects its reward inside the ordinary type scores; and the separate output line invites reading abstention as a seventh, excluded type. `docs/research/longmemeval.md` §6 P2 carries the re-read.) The same file's sibling path has a larger omission, established in §2.3 of the same doc and entered here 2026-08-19: `run_retrieval.py` **indexes user turns only** for every method it ships, and the `has_answer` labels for single-session-assistant questions sit in assistant turns — so that type's gold document set comes out empty and the evaluation drops the question. **All of the paper's published recall/NDCG figures (Tables 3, 4, 9, 10) are computed on 419 of 500 questions, with 91% of the SSA type missing**, while the README discloses only the 30 abstention exclusions; the author confirmed the filter in issue #7. The type deleted is the one the paper's introduction names as the benchmark's differentiator over LoCoMo (LME-A1/A2). The judge is enforced by a hard gate rather than a convention: `assert entry['autoeval_label']['model'] == 'gpt-4o-2024-08-06'` (`:20`) means results judged by any other model are refused outright. And `run_generation.py:177-191` **pops `has_answer` from every turn** before assembling the prompt; that label marks the turns containing the answer, so leaving it in tells the model where to look. |
| **Our handling** | We report both accuracies explicitly and reproduce the abstention slice verbatim, including the overlap — the 30 `_abs` rows staying inside both headlines (called "the double-count" here until the 2026-08-19 correction in the cell above). `JUDGE_MODEL_PIN` / `check_judge_model` fails *before* spending 500 judge calls rather than at aggregation time. The `has_answer` strip is implemented in `longmemeval.render_sessions` on a copy, so turn-level recall labels survive for our own diagnostics while never reaching the prompt. |
| **Proof** | Catalog §10a (LME-1 through LME-5), from the round-6 re-confirmation that our port matched the source. Re-audited 2026-08-05 by re-downloading `print_qa_metrics.py` and reimplementing its metric logic literally, then comparing against `aggregate` over 300 randomized record sets with deliberately unequal type counts and a 25% abstention share: **max divergence 0.000000 pp** on all three of task-averaged, overall and abstention. The five fixes from `8c5f3d6` were each mutation-checked — reinstating the defect in the source makes exactly one named test fail (`test_full_context_never_shows_the_model_which_turns_hold_the_answer`, `test_max_sessions_zero_means_zero_not_all`, `test_task_average_is_taken_over_unrounded_type_means`, `test_an_offpin_judge_fails_before_it_is_ever_called`), so the suite bites rather than merely passing. The sibling-path check that this defect class demands was extended to `ingest`, which discards the evidence label and carries only `session_id`/`date` in `meta`, so the label cannot reach a prompt through retrieval either. |
| **Impact** | Any published LongMemEval number that does not say *which* accuracy it is cannot be compared to another one. The `has_answer` trap deserves separate emphasis because of how it fails: leaking the label surfaces as a **score increase, not an error** — you would "reproduce" the 60.6 full-context baseline while actually solving an easier problem. **Our own first port made exactly this mistake**, which is why it is in this ledger rather than in a list of things other people got wrong. **Status of our port, stated so nobody reads code presence as measurement (2026-08-05): implemented and audited, NOT wired and NEVER RUN.** `src/agmem/bench/longmemeval.py` is 646 lines behind 32 tests, and a caller sweep finds `run_instance`, `load_longmemeval`, `evidence_session_ids` and `upstream_max_history_tokens` with **zero call sites outside those tests**; no script drives it and the dataset is not downloaded, so this repo holds no LongMemEval number of its own and every figure quoted about the benchmark here is upstream's. The gap has teeth of the same shape as the defect above: `max_history_tokens` is opt-in and there is no driver to opt in, so the first caller of `run_instance(full_context=True)` on the `_m` variant would send ~1.5M tokens uncapped. The guard exists; nothing turns it on. Wiring an entry point is a $0 task and a precondition for any future measurement, not a step to discover mid-run. **Added 2026-08-07, on downloading the data: the release that produced the published numbers has been withdrawn by its own author.** `xiaowu0162/longmemeval` is marked deprecated and was replaced the same day (2025-09-19) by `xiaowu0162/longmemeval-cleaned`, which "removes noisy history sessions that interfere with the answer correctness". So the 60.6 full-context baseline above — and every vendor figure in circulation predating that date — was measured on data the author now calls defective, and a run on the current release cannot be compared against any of them. This is the two-accuracy problem one level down: there, a number was uncomparable unless it said *which metric*; here it is uncomparable unless it says *which release*, and no published LongMemEval figure we have found says. Provenance for our own copy (repo sha, file sha256, retrieval date) and the measured consequences are in `docs/research/ace-longmemeval.md` §3. **Refined 2026-08-19, on downloading both releases and diffing them** (`docs/research/longmemeval.md` §3.2): the cleaning was a **pure deletion** — 1,243 filler sessions removed across 454 of 500 `_s` instances, zero sessions added or edited, zero evidence sessions touched, every id/question/answer/type/date field byte-unchanged — and `longmemeval_oracle` is **sha256-identical** across the two releases (`821a2034…`). So the blanket "cannot be compared" above sharpens into three statements: cleaned `_s` scores are structurally **higher** than withdrawn-release scores, making the paper's 60.6/64.0 a *lower bound* on today's data rather than an incomparable number; **oracle-series figures are the only ones comparable across time**, because they are measured on the same bytes; and any `_s` citation must name its release — in practice none does (§5 of the same doc). |
| **Superseded in part (2026-08-19)** | The bolded 2026-08-05 status in the Impact cell — *implemented and audited, NOT wired and NEVER RUN* — is kept as the record of what was true then, and is **no longer true**. The driver exists (`scripts/repro/exp_lme_reading.py`, hard-coding the six guards of `docs/research/longmemeval.md` §8.5), all three releases are on disk (`oracle` / `_s` / `_m`, the last acquired 2026-08-18 behind a streaming loader), and since 2026-08-17 the harness has produced this repo's **own paid LongMemEval numbers**: four oracle reading arms (C4 — with the memory held byte-constant, reading alone moves the official accuracies by 12.57/15.40 pp), `_s` full-context and `_s` retrieval top-50 (C6 — +21.20 pp with zero LLM write calls), a batched `_s` re-baseline, and the `_m` retrieval arm (C7 — the read path's value decays 8.60 pp at 9.9x corpus length), plus judge-swap, history-format and embedding-jitter follow-ups. The "teeth" sentence above also resolved rather than bit: the oracle/`_s` full-context arms send the haystack uncapped *on purpose* — upstream's truncation is a no-op on that data — and before `_m` was measured its acquisition audit fixed the cap's token estimator (4 → measured 4.610) and confirmed the guard fires on 500/500 `_m` instances (0/500 on `_s`). What C-4 itself asserts — two accuracies printed as one, and a label that leaks the answer — governed every one of those runs unchanged: each arm reports `task_averaged` and `overall` separately, and the `has_answer` strip is proven byte-identical to upstream's `prepare_prompt` on 500/500 rendered prompts. The port has grown from the 646 lines stated above to 998. Pre-registrations, results and run facts: `docs/20-lme-reading.md`. |

### C-5 · Citation defects that belong to no one's code

The last class needs no code at all: numbers that travel between papers and change meaning on the way.

The sharpest instance is that **GRAVITY's Table 3 A-Mem row is token-identical to Nemori v4's Table 3** — 912.6K input, 236.8K output, 1,149.4K total. Identical to the digit is not an independent measurement; it is a re-citation, which means the widely repeated claim that "A-Mem's build cost is 10× GRAVITY's" has no independent basis behind it. By our count only Nemori and RecMem genuinely measured that cost, which makes our own measurement the **third**: we record 1,175.3 calls per conversation against Nemori v4's 1,175.5 — agreement to three digits, which is the real result, because it says our write-path *call structure* matches upstream's. Our token count is lower (933.4K versus 1,149.4K, −18.8%), consistent with our more compressed prompts, so ours should be cited as a lower bound rather than as a correction.

Three shorter ones. A-Mem's own §4.3 reports 85–93% token savings, but that figure **counts read context only** and omits the entire write path — which is the path this repo has spent the most effort measuring. RecMem's own table reports **Full Context 84.18 above RecMem's 81.10**, i.e. the strongest baseline still wins, matching what we see with a passthrough configuration. And several third-party claims about these systems remain unverified, so we deliberately do **not** propagate them: HyMem's "A-Mem inference 796.7s, slowest of eight", GRAVITY's unconfirmed A-Mem baseline backbone, and the exact LoCoMo subset behind A-MAC's 225 samples.

### C-6 · *withdrawn*

Published 2026-08-06 and withdrawn the same day: it blamed our harness for A-Mem's read-side query
rewrite, which the pinned clone shows is upstream's own step. The finding survives, correctly
framed, as **B-8**. The number is kept vacant rather than reused, so anyone who read the published
version finds the withdrawal rather than a different entry wearing its name.

### C-7 · Our link-expansion budget was the only cap that ever bound

| | |
|---|---|
| **Paper claim** | A-Mem expands each retrieved note to its linked neighbours, one hop. The paper does not quantify a budget. |
| **Official code** | The budget is **per hit**: the neighbour loop appends and only then breaks on `if j >= k` (`memory_layer.py:895`), so each of the k hits may contribute up to k+1 neighbours, and duplicates are neither skipped nor free — a repeated link is served twice and burns a slot each time (`:889-897`). |
| **Our handling** | We spent a single budget of 5 across all hits. The shape is now a config knob (`AgmemConfig.link_expansion_per_hit`, `LinkExpansion(cap, per_hit)`), the faithful arm is `amem_perhit` in `scripts/repro/configs.py`, and the four-way table's A-Mem row runs it. The duplicate half is **disclosed, not fixed**: `RetrievalPipeline._assemble` dedups on `(memory_type, id)` before anything reaches the bundle, an invariant every methodology depends on, so reproducing upstream's double-serving would mean changing shared machinery for one arm. |
| **Proof** | `tests/test_pipeline_p0.py::test_link_expansion_per_hit_budget_is_not_the_global_one` distinguishes the two modes behaviourally; `tests/test_repro_artifacts.py::test_amem_perhit_pins_the_cap_shape_and_leaves_other_arms_alone` pins the arm and asserts no other config moved. Measurement: `gpt-4o-mini_amem_perhit_all_k10_ours_expand-on_run1_e3sPH.json` against `..._all_k10_ours_expand-on_run1_e3s.json`, stamps differing only in `config`. |
| **Measured (2026-08-06)** | **J 59.87 → 61.23 (+1.36)**, F1 +1.51, BLEU-1 +1.52. Notes served per question 15.0 → 26.7, context 1,913 → 3,322 tokens (+74%), eval cost +$0.42. The mechanism is breadth, not precision: abstention barely moves (26.9% → 25.1%), accuracy-when-answered is flat (82.0% → 81.7%), and the gain concentrates on **open-domain (+5.21)** with **temporal exactly +0.00** — the inverse of the embedder and query-rewrite findings, which both gained least on open-domain. Single seed. |
| **Impact** | On our numbers, not upstream's: every A-Mem result this repository published before today was produced with a read budget one third the size of the one A-Mem's own code grants. Worth stating precisely, because the naive reading is backwards — **upstream's per-hit cap never binds at all.** A note cannot hold more than 5 links, since the write path retrieves only 5 neighbour candidates (`memory_layer.py:755`; ours `AMemOrganizer(top_k=5)`), and this store measures 5,882 notes / 18,886 links / mean 3.21 with the distribution stopping dead at 5. Upstream's shape therefore means "serve every link of every hit"; ours was the only cap doing any cutting, saturating on every question. |

All four cells of the 2×2 with B-8 were run, so the interaction is measured rather than assumed: dropping the query rewrite is worth +5.26 at our global-5 budget but only +4.35 at the faithful per-hit one, and opening the budget is worth +1.36 with the rewrite in place but only +0.45 without it. Summed deltas predict 66.49; the measured both-changes cell is **65.58**, an interaction of **−0.91**. Both changes buy coverage, so whichever arrives second finds less left to buy — the abstention series is 26.9% → 21.6% → 21.0% with accuracy-when-answered flat at 83.0%. The deployment reading differs from the fidelity one and is worth stating: raw question at our *old* global-5 budget scores 65.13 for 60% of the context (1,937 vs 3,283 tokens/question), so +0.45 J for +69% context is a bad trade, and the configuration that best serves a deployment is the least faithful of the four.

### C-8 · Our memory snapshot had a hardcoded type roster, and left a whole retrieved type out of the inventory

| | |
|---|---|
| **Paper claim** | Ours, not a paper's: this campaign's own rule is that every run persists a complete artifact set — LLM I/O, retrieval, **memory snapshot**, timing and cost — so that no measurement ever has to be bought twice. The snapshot's contract is to be the store's inventory. |
| **Official code** | The offending code is ours. `SNAPSHOT_ITEM_TYPES` in `scripts/exp_amem_repro.py` was a literal tuple, written when every arm served one or two memory types. Zep serves three. `communities` was not in it, so the Zep campaign's `memory.jsonl` came out **1,243 rows short** and its summary's `memory_capacity` reported **17,259 items where the store held 18,502**. |
| **Our handling** | `communities` is in the roster. The roster stays a literal on purpose — it covers types belonging to methodologies that have no LoCoMo arm, so deriving it from the arm table would *shrink* it — and the coverage property is enforced from the other side instead: a test computes the set of types the configured arms retrieve and fails if any is missing from the roster, so the next arm that brings a new type cannot reintroduce the gap silently. The `docs/18` cell is corrected in place with the old value named; the Zep *ingest* summary still carries the short count because it predates the fix, while the five recipe-sweep summaries record all four types. |
| **Proof** | `tests/test_repro_artifacts.py::test_every_memory_type_an_arm_can_serve_is_in_the_snapshot`, which fails the moment any arm serves a type the writer would drop. The discrepancy is visible directly in the artifacts: `..._e3sZ.json` records three types, `..._e3sZrrf.json` four, against one identical store. |
| **Impact** | It failed in the direction that does not announce itself. The op log counted all 1,243 `ADD:communities`, the read path served communities on every question, and every J / F1 / cost figure comes from records and traces — nothing downstream reads `memory_capacity`, so no measurement was wrong *because* of this and nothing crashed. The single consumer was a human reading a capacity column, and `docs/18-locomo-4way.md` was that consumer: it reported Zep's surviving items as **11,377** where the answer is **12,620**. Small in consequence, entered for its shape — a hardcoded inventory of "what to record" is a standing claim that no future arm will produce anything new, and that claim expired the first time an arm brought a third subgraph. |

### C-9 · ACE/FiNER: two accuracies are printed as one number, and a failed sample leaves the denominator

| | |
|---|---|
| **Paper claim** | ACE states its online adaptation result on FiNER as a single accuracy (69.1% → 81.9%), and the release's harness prints and saves one field called `accuracy`. |
| **Official code** | There are two, and they differ by a factor of three on real data. `evaluate_test_set` builds one dict holding `accuracy` — the **tag-level** micro rate `correct_tags / total_tags` from `_evaluate_finer_accuracy` (`eval/finance/data_processor.py:183-200`) — beside `correct` and `total`, which are **sample-level** exact match, incremented as `1 if is_correct else 0` (`utils.py:253-254`) where `is_correct` requires all four tags (`score == 1`, `data_processor.py:152`). The summary line then prints them as one fact: `f"Final Accuracy: {accuracy:.3f} ({correct}/{total})"` (`utils.py:286`) — **the parenthetical does not equal the number in front of it**. The saved online result repeats the pairing (`"accuracy": final_test_accuracy` with `"correct": correct_count_sample_based`, `ace.py:1097-1098`), so a reader who recomputes `correct/total` from the artifact gets a different number than the artifact's own headline; a third quantity, also labelled "Accuracy", prints every 50 samples (`utils.py:269`). Separately, a sample that raises is caught and returned as `(None, msg)` (`utils.py:198-199`), then printed and `continue`d (`:248-250`), so it never reaches `total`, `answers` or `targets` — **failures shrink the population instead of scoring zero**. Three further scorer defects are cited at the port site (`src/agmem/bench/finer.py`): `eval()` runs on model output inside a bare `except`; over-prediction is truncated away unscored while under-prediction is padded and penalised; and the training signal (all-or-nothing) is not the reported metric (per-tag). |
| **Our handling** | `aggregate` returns **both**, always, under names that say which is which — the same rule the LongMemEval port follows for the same reason (C-4). An unanswerable sample is scored wrong and counted in `n_failed`, so the denominator is the split. Upstream's tag arithmetic is otherwise reproduced exactly, including the over-prediction leniency, because that is the metric ACE's number is stated in; the discarded count is surfaced as `n_over` rather than fixed. |
| **Proof** | `tests/test_bench_finer.py` — `test_the_two_accuracies_are_different_numbers_and_both_are_reported`, `test_a_failed_sample_costs_accuracy_instead_of_leaving_the_split`, `test_over_prediction_is_free_in_upstreams_metric_and_we_surface_it`, and four more. Measured on our own run: the same 441 samples score **48.24** tag-level and **16.10** sample-level. |
| **Impact** | Any citation of a FiNER accuracy — ACE's or anyone's — is uninterpretable unless it says which of the two it is, and the two are far enough apart that the choice is not a rounding matter. This is the third entry in this ledger where one benchmark's harness computes several quantities and reports one name (with C-2 and C-4), which is why "which accuracy?" is now a standing question this project asks of every ported harness before running it. A dataset note found the same way: FiNER's gold labels are internally inconsistent in case (`ShareBased…` and `Sharebased…` in the same row for the same element) — harmless only because the scorer lowercases both sides. |

### C-10 · Our vector stores key on the bare id while the doc stores key on (id, memory_type)

| | |
|---|---|
| **Paper claim** | Ours, not a paper's: the retrieval pipeline's stated invariant is that the items table is keyed `(id, memory_type)` and the same id under two types is **two distinct items** — the comment at `src/agmem/retrieval/pipeline.py:252` says exactly this, and the dedup there is built on it. |
| **Official code** | The offending code is ours, and it holds only half the invariant. Both doc stores do key items compositely (`PRIMARY KEY (id, memory_type)` — `src/agmem/stores/sqlite_doc.py:43`, `src/agmem/stores/postgres_doc.py:49`), but **all five vector backends key on `item_id` alone**: numpy's position map, sqlite-vec's `item_id TEXT NOT NULL UNIQUE` (`sqlite_vec.py:51`), chroma's `ids=[item_id]` upsert (`chroma_vec.py:72`), lance's delete-then-add on `id` (`lance_vec.py:68`), and qdrant's `uuid5(item_id)` point id (`qdrant_vec.py:71`) — `memory_type` rides along as filterable metadata, never as identity. The write side completes the hazard: `INVALIDATE` and `DELETE` in `src/agmem/memory.py:794` and `:829` call `vector_store.delete([op.target_id])` with the bare id and no type. So if one id ever existed under two memory types, the second upsert would silently **overwrite the first type's vector** (leaving its doc row intact but served under the wrong type's embedding), and an invalidate of either would **delete the other's vector too**. |
| **Our handling** | Deliberately deferred, not fixed. Rekeying vector identity to `(id, memory_type)` would orphan every vector store already persisted on disk — the campaign artifacts' stores would stop resolving, and rebuilding them is re-ingest spend, which the artifact-capture rule exists to avoid. The hazard is disclosed at the code sites as of 2026-08-19, and the entry exists so the deferral is a recorded decision rather than an unnoticed gap. |
| **Proof** | The keying is directly visible at the cited lines; no reproduction script is needed because the claim is about schemas and call signatures, not behavior under load. The impact bound rests on an id sweep: every live item id is minted by `new_id()` = `uuid.uuid4().hex` (`src/agmem/core/types.py:20`), and the only non-uuid ids in the codebase — `memoryos:user_profile` (`organizers/memoryos/organizer.py:137`) and the `consolidate:*` cursor keys (`organizers/base.py:234`) — are a doc-store-only document and doc-store-only cursors, **never embedded**, so no id has ever existed under two types with a vector attached. |
| **Impact** | On measurements taken so far, ~0 — the collision precondition (one id, two embedded types) has never occurred, so no published number moved. Entered for its shape, the same shape as C-8 and the `getattr`-guard finding: **a declared invariant that only some layers enforce is a standing claim about future writers**, and the first organizer that mints deterministic or externally-derived ids (as MemoryOS already does for its unembedded profile) would cross the precondition without any test failing. The failure would arrive as C-8's did — in the direction that does not announce itself: a wrong-type vector serving quietly, or a sibling type's recall degrading after an invalidate. |

### Considered and not entered · the `np.float32` crash that destroyed a completed run's artifacts

The `np.float32` leak that destroyed the `zep_mmr` run's artifacts (`MMRReranker` returned numpy scalars into a score contract typed as `float`; `json.dumps` had no encoder for them, and the sidecar write took the summary down with it after the eval had completed and been paid for) is **not a ledger entry**, deliberately. The ledger's bar is *a number that was reported and is wrong*, and no number ever was: the run's metrics were printed before the crash, the records were rebuilt from its own LLM trace with no model calls, and the reconstruction refuses to write unless it reproduces those metrics exactly — which it did, and which the bootstrap then recomputed from the rebuilt records a second time. The defect, its three-week latency (it dated from `518f788` and no arm had ever selected MMR), and the one thing that was genuinely lost — the structured per-question `retrieval` capture, which a trace cannot restore — are disclosed at the point of use in `docs/18-locomo-4way.md` rather than here. Recorded so that "should this be a ledger entry?" has a written answer.

---

## Cross-cutting patterns

These five are findings in their own right — they generalize past this repo, and each has been adopted as a project stance rather than left as an observation.

1. **"Paper ≠ official code" is the rule, not the exception.** A-Mem's extraction step is dead in two of its three repositories; Nemori's §3.3.3 is absent from the code; MemoryOS's paper constants are not the constants that produced its numbers, and its lowest-heat eviction is in *neither* codebase; G-Memory's §4.3 summary function is absent, the code being a regex critique finetune instead; ACE's grow-and-refine is thin, ADD-only with a silently skipped dedup; A-MAC's weights are unpublished and were fitted on top of defects. **Zep is the sole reverse case**, with code running ahead of its paper. The consequence adopted here: *"faithful to the paper" cannot be a single target*, so every divergent constant names the lineage it came from, and the lineage is a switchable, tested object (`NEMORI_PRESETS`, `MEMORYOS_PRESETS`, `ZEP_SEARCH_RECIPES`, `MEMMACHINE_PRESETS`). **Pin the lineage** is the only defensible position, and a run states which system it measured.

2. **Dead knobs stay dead.** Values that read as configuration but are never loaded are everywhere: A-MAC's `X_train`/`y_train` and five more; MemMachine's `QueryPolicy`, all six fields, confirmed by `grep -rn 'policy\.'` returning nothing; MemoryOS's `query_keywords = set()`, its `lambda_t` in all three copies, its eval `R_recency`, its dead profile-update prompt; Nemori's four; ReasoningBank's MaTTS correctness label, which is both dead **and** inverted relative to its sibling file; ACE's default-off dedup analyzer. The rule adopted: **reproduce dead knobs as dead, never resurrect them.** Resurrecting one silently changes the cost and behavior of the thing you claim to be reproducing — and the single violation of this rule was ours (B-3), caught in round 12 and reverted. **The rule has exactly one boundary, found in A-5**: it presumes the surrounding code *runs*. ReasoningBank's MaTTS induction does not — it raises before reaching its model — so there is no shipped behavior to be faithful to, and the referent falls back to the paper plus the prompt text. A dead knob can be reproduced as dead; a dead entry point cannot be reproduced at all, and saying "release-faithful" of such a path would be a category error rather than a difficult choice.

3. **"Which copy produced the number?" is a question with an answer, and it must be asked.** MemoryOS ships three cores with different constants and different formulas (issue #73 records the drift as a real incident), A-Mem three repositories with a plain-vs-robust split inside one, Nemori two paper versions whose ablation conclusions *reverse* between them, MemMachine two backends across two harnesses, Graphiti a codebase in continuous drift from a single 2025 paper. A verdict reached by reading only one copy is a distinct, nameable defect class, and several early findings in this project failed exactly that way.

4. **"Which path did the harness actually call?" is a different question from "what does this code do."** It is answered by following call sites, not by reading files, and it is where the highest-value findings came from: MemMachine's published path is `declarative` with short-term memory disabled, and neither harness runs at all (A-3); G-Memory's three MAS workflows **discard the failed-trajectory list** at the call site, so failures never reach read time despite the signature suggesting otherwise, and the harness constants are `1 / 3 / 0.0`, not the function's Python defaults; MemoryOS's eval driver passes a mid-term capacity of 2000 against a class default of 7; A-Mem's plain-versus-robust split is invisible until you trace the import.

5. **Most of the felt distance from a paper is naming, not semantics.** Three generations of vocabulary stack up — paper rhetoric, upstream engineering name, our mechanism name (segment = session; Profile Memory = `semantic_memory` = `profile.py`; Local Message Partitioning = `BatchSegmenter` = `BatchPartitioner`). The mitigation is mechanical and cheap: every organizer and stage docstring carries the paper §-number and the original term, so a reviewer can check the mapping instead of inferring it. **One class of text is exempt, and 2026-08-10 is why: a prompt is not naming, it is the mechanism.** Our ACE port paraphrased two sentences of upstream's reflector and curator prompts — who the diagnosis is about, and who the playbook is for — and the curator went from writing tagging knowledge to writing staff-training recommendations (B-6). Nothing in the code differed; the operations, the schema, the dedup and the call structure were all faithful. Prompts are now transcribed, not paraphrased, and where a divergence is deliberate it is named at the code site like any other.

---

## Status of this ledger

Entries here are outcomes; the process logs that produced them are the twelve fidelity rounds under `docs/research/`, and the per-entry evidence base with its impact tags is `docs/research/upstream-defect-catalog.md`. Remediation was carried out in eight per-methodology commits (`ed44610` A-MAC, `6fad7bb` G-Memory, `a3bf258` Zep, `1f4df42` MemMachine, `7ced0eb` MemoryOS, `688c959` Nemori, `3038510` A-Mem, `eba9e97` ACE/ReasoningBank), which took the suite from 388 to 441 passing tests — the added 53 being mostly pinning tests written so that a fidelity property cannot regress silently.

The Tier-0 proofs in `scripts/repro/defects/` are the deterministic half of the evidence: they cost nothing, make no model calls, and either prove their claim or fail CI. They deliberately self-skip with a `SKIP:` line when their evidence is absent — an upstream clone that was not fetched, or a heavy dependency the core install omits — following the same capability-gating convention as the test suite, so that a missing prerequisite never masquerades as a passing proof. The API-measured half has since started arriving, and its entries carry a **Measured** row alongside the five-field schema: B-3 (Nemori's dead merge filter, across seed, model and embedder axes), A-4 (Mem0's OSS structure under our harness), B-8 (the cost of A-Mem's own read-side keyword rewrite) and C-7 (the cost of our own link-expansion budget). Where a number would belong and none has been paid for, there is still no number — that remains the rule, not an aspiration. Measured rows state their seed count, and a single-seed result is labeled as one.
